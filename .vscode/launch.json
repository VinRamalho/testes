{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "api",
            "request": "launch",
            "command": "yarn start",
            "cwd": "${workspaceRoot}"
        }
    ]
}
